# Cloudflare Pages Configuration
# This file is optional - configuration is typically done in Cloudflare Dashboard
# But can be used for local wrangler pages dev/deploy

name = "marketlens"
compatibility_date = "2026-04-01"

[build]
command = "npm run build"
cwd = "."

[build.upload]
format = "service-worker"
